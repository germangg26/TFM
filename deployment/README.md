# Deployment

Reserva esta carpeta para assets y configuraciones de despliegue.

- `api/`: componentes para servir el recomendador online (ejemplo FastAPI).
- `export_artifacts.py` / `export_propensity.py`: generan los artefactos que consume la API.
